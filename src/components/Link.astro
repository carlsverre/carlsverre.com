---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a"> & {
  href: string;
  label?: string;
};

const { href, label, ...props } = Astro.props;
---

<a href={href} aria-label={label} data-astro-prefetch {...props}><slot /></a>
